name: SSHX Session with Telegram and Tools

on:
  workflow_dispatch:

jobs:
  sshx:
    runs-on: ubuntu-latest

    steps:
      - name: Run SSHX with Backup + Telegram
        env:
          BOT_TOKEN: ${{ secrets.BOT_TOKEN }}
          CHAT_ID: ${{ secrets.CHAT_ID }}
        run: |
          send_telegram() {
            curl -s -X POST "https://api.telegram.org/bot${BOT_TOKEN}/sendMessage" \
              -d chat_id="${CHAT_ID}" \
              -d parse_mode=Markdown \
              -d text="$1"
          }

          send_telegram "🚀 *GitHub SSHX Session Started!* Initializing setup..."

          echo "⚙️ Launching SSHX session"
          curl -sSf https://sshx.io/get | sh -s run -- bash -c '
            set -e  # Fail fast inside SSHX script

            echo "🔧 Updating system"
            apt update && apt upgrade -y

            echo "🛠 Installing tools"
            apt install -y git curl vim zip tar

            echo "📁 Creating project directory"
            mkdir -p /root/my-project
            cd /root/my-project

            echo "🗃 Backing up files (if any)"
            if [ "$(ls -A .)" ]; then
              zip -r /root/backup_$(date +%Y%m%d_%H%M%S).zip . || echo "Backup failed"
            else
              echo "No files to backup"
            fi

            echo "🧪 Simulating restore"
            BACKUP_FILE=$(ls /root/backup_*.zip 2>/dev/null | tail -n1)
            if [ -f "$BACKUP_FILE" ]; then
              unzip "$BACKUP_FILE" -d /root/restored_files
              echo "✅ Restored to /root/restored_files"
            fi

            echo "✅ SSHX session setup complete!"
            bash
          ' || send_telegram "❌ *SSHX Failed to Start or Crashed.* Check your workflow logs!"

          send_telegram "✅ *SSHX Session Completed!* Tools installed, backup created/restored."
